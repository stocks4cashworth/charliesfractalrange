function onOpen() {
  const ui = SpreadsheetApp.getUi();
  
  ui.createMenu('🚀 Hurst Calculations')
      .addItem('hurst', 'calculateHurstMatrix')
      .addItem('hurstbyperiod', 'calculateMasterFractalMatrix')
      .addItem('Volatility', 'calculatePriceAndVolumeVolatility')
      .addSeparator()
      .addItem('createUnifiedChart', 'createUnifiedChart')
      .addSeparator()
      .addItem('populateRollingHurstColumns', 'populateRollingHurstColumns')
      .addSeparator()

      .addToUi();

}
