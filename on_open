function onOpen() {
  const ui = SpreadsheetApp.getUi();
  
  ui.createMenu('🚀 Hurst Calculations')
      .addItem('hurst', 'generateDailyRollingFractalMatrix')
      .addItem('hurst chart', 'generateSplitHurstTrendsCharts')
      
      .addSeparator()
      .addItem('Main Sheet Updates', 'generateMainSummaryDashboard')
      .addSeparator()
      .addItem('forward chart', 'generateFractalDeviationChart')
      .addSeparator()

      .addToUi();

}
